# Runner Passes Admin V2

<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

ระบบจัดการ Runner Passes สำหรับการแข่งขันวิ่ง

## 📋 สารบัญ

- [การติดตั้งและรันโปรเจกต์](#การติดตั้งและรันโปรเจกต์)
- [การ Deploy บน Vercel](#การ-deploy-บน-vercel)
- [การตั้งค่า Domain ที่ Namecheap](#การตั้งค่า-domain-ที่-namecheap)
- [การตั้งค่า Environment Variables](#การตั้งค่า-environment-variables)
- [Troubleshooting](#troubleshooting)

---

## 🚀 การติดตั้งและรันโปรเจกต์

### Prerequisites

- Node.js (เวอร์ชัน 18 ขึ้นไป)
- npm หรือ yarn

### ขั้นตอนการติดตั้ง

1. **Clone repository:**
   ```bash
   git clone <repository-url>
   cd runnerbib_prd
   ```

2. **ติดตั้ง dependencies:**
   ```bash
   npm install
   ```

3. **สร้างไฟล์ `.env.local`:**
   ```bash
   cp .env.example .env.local
   ```

4. **ตั้งค่า Environment Variables:**
   - เปิดไฟล์ `.env.local`
   - ใส่ค่าต่างๆ ที่จำเป็น:
     ```
     VITE_SUPABASE_URL=your_supabase_url
     VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
     ```

5. **รันโปรเจกต์:**
   ```bash
   npm run dev
   ```

6. **เปิดเบราว์เซอร์:**
   - ไปที่ `http://localhost:5173`

---

## 🌐 การ Deploy บน Vercel

### ขั้นตอนที่ 1: เตรียมโปรเจกต์

1. **ตรวจสอบว่าโปรเจกต์พร้อม deploy:**
   ```bash
   npm run build
   ```
   ถ้า build สำเร็จ แสดงว่าโปรเจกต์พร้อม deploy

2. **Commit โค้ดทั้งหมด:**
   ```bash
   git add .
   git commit -m "Prepare for deployment"
   git push origin main
   ```

### ขั้นตอนที่ 2: สร้างบัญชี Vercel

1. ไปที่ [https://vercel.com](https://vercel.com)
2. คลิก **"Sign Up"** หรือ **"Log In"**
3. **แนะนำ:** ใช้ GitHub account (ง่ายและสะดวกที่สุด)

### ขั้นตอนที่ 3: สร้างโปรเจกต์ใหม่บน Vercel

1. หลังจาก Login แล้ว คลิก **"Add New..."** → **"Project"**
2. **Import Git Repository:**
   - เลือก Git provider (GitHub, GitLab, Bitbucket)
   - เลือก repository ที่ต้องการ deploy
   - คลิก **"Import"**

### ขั้นตอนที่ 4: ตั้งค่า Build Configuration

Vercel จะ auto-detect Vite project โดยอัตโนมัติ แต่ให้ตรวจสอบว่า:

- **Framework Preset:** Vite
- **Root Directory:** `.` (หรือปล่อยว่างไว้)
- **Build Command:** `npm run build`
- **Output Directory:** `dist`
- **Install Command:** `npm install`

### ขั้นตอนที่ 5: ตั้งค่า Environment Variables

**สำคัญมาก!** ต้องตั้งค่า Environment Variables ก่อน deploy:

1. ในหน้า Project Settings → **Environment Variables**
2. คลิก **"Add New"** เพื่อเพิ่มตัวแปรใหม่

**ตัวแปรที่ต้องเพิ่ม:**

| Key | Value | Environment |
|-----|-------|-------------|
| `VITE_SUPABASE_URL` | URL ของ Supabase project (เช่น `https://xxxxx.supabase.co`) | Production, Preview, Development |
| `VITE_SUPABASE_ANON_KEY` | Anon Key ของ Supabase project | Production, Preview, Development |

**ตัวอย่างการกรอก:**
```
Key: VITE_SUPABASE_URL
Value: https://abcdefghijklmnop.supabase.co

Key: VITE_SUPABASE_ANON_KEY
Value: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### ขั้นตอนที่ 6: Deploy

1. คลิก **"Deploy"**
2. รอให้ build เสร็จ (ประมาณ 1-3 นาที)
3. เมื่อเสร็จแล้ว จะได้ลิงก์ Production URL เช่น:
   ```
   https://your-project-name.vercel.app
   ```

### ขั้นตอนที่ 7: Auto Deploy (ตั้งค่าอัตโนมัติ)

หลังจาก deploy ครั้งแรกแล้ว:
- ทุกครั้งที่คุณ **push code ไปที่ GitHub** → Vercel จะ **deploy อัตโนมัติ**
- ถ้า push ไปที่ `main` branch → จะ deploy ไปที่ **Production**
- ถ้า push ไปที่ branch อื่น → จะได้ **Preview URL** แยก

---

## 🌍 การตั้งค่า Domain ที่ Namecheap

### ขั้นตอนที่ 1: สร้างบัญชี Namecheap

1. ไปที่ [https://www.namecheap.com](https://www.namecheap.com)
2. คลิก **"Sign Up"** ที่มุมขวาบน
3. กรอกข้อมูลและยืนยันอีเมล

### ขั้นตอนที่ 2: ค้นหาและซื้อ Domain

1. หลังจาก Login แล้ว ไปที่หน้าแรก
2. ในช่องค้นหา Domain พิมพ์ชื่อ domain ที่ต้องการ (เช่น `mywebsite.com`)
3. คลิก **"Search"**
4. เลือก domain ที่ต้องการและคลิก **"Add to Cart"**
5. ตั้งค่า:
   - เลือกระยะเวลา (1 ปี, 2 ปี, 3 ปี)
   - **WhoisGuard** (แนะนำให้เปิด - ป้องกันข้อมูลส่วนตัว)
   - **Auto-Renew** (แนะนำให้เปิด - จะต่ออายุอัตโนมัติ)
6. คลิก **"Checkout"** และชำระเงิน

---

## ⚙️ การเชื่อม Domain กับ Vercel

### ขั้นตอนที่ 1: เพิ่ม Domain ใน Vercel

1. ไปที่ Vercel Dashboard
2. เลือก Project ของคุณ
3. ไปที่ **Settings** → **Domains**
4. คลิก **"Add Domain"**
5. พิมพ์ domain ที่ต้องการ (เช่น `example.com`)
6. คลิก **"Add"**

### ขั้นตอนที่ 2: ตั้งค่า DNS ที่ Namecheap

**วิธีที่ 1: ใช้ Nameservers ของ Vercel (แนะนำ - ง่ายที่สุด)**

1. **Copy Nameservers จาก Vercel:**
   - ในหน้า Domains ของ Vercel จะแสดง Nameservers เช่น:
     ```
     ns1.vercel-dns.com
     ns2.vercel-dns.com
     ```

2. **ตั้งค่า Nameservers ที่ Namecheap:**
   - ไปที่ Namecheap → **Domain List**
   - คลิก **"Manage"** ที่ domain ที่ต้องการ
   - ไปที่ **"Nameservers"**
   - เลือก **"Custom DNS"**
   - ใส่ Nameservers จาก Vercel:
     ```
     ns1.vercel-dns.com
     ns2.vercel-dns.com
     ```
   - คลิก **"Save"**

**วิธีที่ 2: ใช้ DNS Records**

1. **Copy DNS Records จาก Vercel:**
   - ในหน้า Domains ของ Vercel จะแสดง DNS Records ที่ต้องตั้งค่า
   - Copy ลิงก์ CNAME ที่ Vercel ให้มา (เช่น `cname.vercel-dns.com`)

2. **ตั้งค่า DNS Records ที่ Namecheap:**
   - ไปที่ Namecheap → **Domain List** → **Advanced DNS**
   - ลบ DNS Records เดิม (ถ้ามี)
   - เพิ่ม CNAME Record สำหรับ root domain:
     - **Host:** `@` (หรือปล่อยว่าง - สำหรับ root domain)
     - **Type:** เลือก **CNAME Record**
     - **Value:** ใส่ลิงก์ Vercel ที่ copy มา (เช่น `cname.vercel-dns.com`)
     - **TTL:** `Automatic`
     - คลิก **"Save"** (เครื่องหมายถูก)
   - เพิ่ม CNAME Record สำหรับ www (ถ้าต้องการ):
     - **Host:** `www`
     - **Type:** เลือก **CNAME Record**
     - **Value:** ใส่ลิงก์ Vercel เดียวกัน (เช่น `cname.vercel-dns.com`)
     - **TTL:** `Automatic`
     - คลิก **"Save"**
   - เพิ่ม CNAME Record สำหรับ subdomain อื่นๆ (เช่น `bib`):
     - **Host:** `bib` (สำหรับ subdomain bib.example.com)
     - **Type:** เลือก **CNAME Record**
     - **Value:** ใส่ลิงก์ Vercel เดียวกัน (เช่น `cname.vercel-dns.com`)
     - **TTL:** `Automatic`
     - คลิก **"Save"**

**หมายเหตุ:** 
- Namecheap รองรับ CNAME สำหรับ root domain (@) ผ่าน ALIAS record หรือ CNAME flattening ซึ่งจะทำงานเหมือน CNAME แต่ใช้ได้กับ root domain
- สำหรับ subdomain (เช่น `bib`, `www`) สามารถใช้ CNAME Record ได้โดยตรง

### ขั้นตอนที่ 3: เพิ่ม www Domain

1. ในหน้า Domains ของ Vercel
2. คลิก **"Add Domain"** อีกครั้ง
3. พิมพ์ `www.example.com` (ใส่ www นำหน้า)
4. Vercel จะตั้งค่าให้อัตโนมัติ

### ขั้นตอนที่ 4: รอ DNS Propagation

1. หลังจากตั้งค่า DNS แล้ว ต้องรอให้ DNS propagate
2. **เวลาที่ใช้:**
   - **เร็วที่สุด:** 5-10 นาที
   - **ปกติ:** 1-24 ชั่วโมง
   - **นานที่สุด:** 48 ชั่วโมง

3. **ตรวจสอบสถานะ:**
   - ไปที่ Vercel Dashboard → Domains
   - จะเห็นสถานะ:
     - ✅ **Valid Configuration** = ตั้งค่าถูกต้อง
     - ⏳ **Pending** = กำลังรอ DNS propagate
     - ❌ **Invalid Configuration** = ตั้งค่าผิดพลาด

---

## 🔧 การตั้งค่า Environment Variables

### สำหรับ Local Development

สร้างไฟล์ `.env.local` ใน root directory:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

### สำหรับ Vercel Production

1. ไปที่ Vercel Dashboard → Project → Settings → Environment Variables
2. เพิ่มตัวแปรตามตารางด้านบน
3. เลือก Environment ทั้งหมด (Production, Preview, Development)
4. คลิก Save

---

## 🐛 Troubleshooting

### ❌ ปัญหา: Domain ไม่ทำงานหลังจากตั้งค่า DNS แล้ว

**วิธีแก้:**

1. **ตรวจสอบ DNS Records:**
   - ไปที่ Namecheap → Advanced DNS
   - ตรวจสอบว่าตั้งค่าถูกต้องหรือไม่
   - ตรวจสอบว่า Value ตรงกับที่ Vercel ให้มาหรือไม่

2. **ตรวจสอบ Nameservers:**
   - ถ้าใช้ Nameservers ตรวจสอบว่าตั้งค่าถูกต้องหรือไม่
   - ต้องเป็น `ns1.vercel-dns.com` และ `ns2.vercel-dns.com`

3. **รอ DNS Propagation:**
   - DNS อาจใช้เวลา 24-48 ชั่วโมง
   - ใช้ [dnschecker.org](https://dnschecker.org) ตรวจสอบว่า DNS propagate ไปทั่วโลกแล้วหรือยัง

4. **ตรวจสอบที่ Vercel:**
   - ไปที่ Vercel Dashboard → Domains
   - ดูว่ามี error message อะไรหรือไม่

### ❌ ปัญหา: www ไม่ทำงาน แต่ root domain ทำงาน

**วิธีแก้:**

1. **ตรวจสอบ CNAME Record:**
   - ไปที่ Namecheap → Advanced DNS
   - ตรวจสอบว่ามี CNAME Record สำหรับ `www` หรือไม่
   - Value ต้องเป็น `cname.vercel-dns.com`

2. **เพิ่ม www Domain ใน Vercel:**
   - ไปที่ Vercel Dashboard → Domains
   - เพิ่ม `www.example.com` (ใส่ www นำหน้า)

### ❌ ปัญหา: SSL Certificate ไม่ทำงาน

**วิธีแก้:**

1. **รอให้ Vercel สร้าง SSL Certificate:**
   - Vercel จะสร้าง SSL Certificate อัตโนมัติหลังจาก DNS propagate แล้ว
   - ใช้เวลา 5-10 นาที

2. **ตรวจสอบที่ Vercel:**
   - ไปที่ Domains → คลิกที่ domain
   - ดูสถานะ SSL Certificate

3. **ถ้ายังไม่ทำงาน:**
   - ลบ domain ออกจาก Vercel
   - รอ 5 นาที
   - เพิ่ม domain ใหม่

### ❌ ปัญหา: Build ล้มเหลว

**วิธีแก้:**

1. **ตรวจสอบ Environment Variables:**
   - ตรวจสอบว่าตั้งค่า Environment Variables ครบหรือยัง
   - ตรวจสอบว่าค่าถูกต้องหรือไม่

2. **ตรวจสอบ Build Logs:**
   - ไปที่ Vercel Dashboard → Deployments
   - คลิกที่ deployment ที่ล้มเหลว
   - ดู Build Logs เพื่อหาสาเหตุ

3. **ทดสอบ Build Local:**
   ```bash
   npm run build
   ```
   ถ้า build ไม่ผ่านที่ local ก็จะไม่ผ่านที่ Vercel เช่นกัน

### ❌ ปัญหา: 404 NOT_FOUND - DEPLOYMENT_NOT_FOUND

**Error Message:**
```
404: NOT_FOUND
Code: DEPLOYMENT_NOT_FOUND
ID: sin1::p67qc-xxxxx-xxxxx
```

**สาเหตุที่เป็นไปได้:**

1. **Deployment ถูกลบหรือหมดอายุแล้ว**
   - Vercel จะเก็บ deployment ไว้ชั่วคราว
   - Preview deployments อาจถูกลบอัตโนมัติหลังจากไม่ใช้งาน

2. **Deployment ID ไม่ถูกต้อง**
   - URL ที่ใช้เข้าถึง deployment อาจผิดพลาด
   - หรือ deployment ถูกสร้างใหม่แล้ว

3. **Project ถูกลบหรือย้าย**
   - Project อาจถูกลบหรือย้ายไปทีมอื่น

**วิธีแก้:**

1. **ตรวจสอบ Deployments ใน Vercel Dashboard:**
   - ไปที่ Vercel Dashboard → เลือก Project ของคุณ
   - ไปที่แท็บ **"Deployments"**
   - ตรวจสอบว่ามี deployment ล่าสุดหรือไม่
   - ถ้าไม่มี → ต้อง deploy ใหม่

2. **Deploy ใหม่:**
   - ไปที่ Vercel Dashboard → Project → **"Deployments"**
   - คลิก **"Redeploy"** ที่ deployment ล่าสุด (ถ้ามี)
   - หรือ push code ใหม่ไปที่ GitHub:
     ```bash
     git add .
     git commit -m "Redeploy"
     git push origin main
     ```
   - Vercel จะ deploy อัตโนมัติ

3. **ตรวจสอบ Production URL:**
   - ไปที่ Vercel Dashboard → Project → **"Settings"** → **"Domains"**
   - ตรวจสอบ Production URL ที่ถูกต้อง
   - ใช้ URL นี้แทน URL ของ deployment ที่ล้มเหลว

4. **ตรวจสอบ Project Status:**
   - ไปที่ Vercel Dashboard
   - ตรวจสอบว่า Project ยังอยู่และไม่ได้ถูกลบ
   - ถ้า Project หาย → ต้อง Import Project ใหม่

5. **ลบและ Deploy ใหม่ (ถ้าจำเป็น):**
   - ถ้ายังแก้ไม่ได้ ลองลบ deployment ที่มีปัญหา
   - แล้ว deploy ใหม่โดย:
     - Push code ใหม่ไปที่ GitHub
     - หรือใช้ Vercel CLI:
       ```bash
       npm i -g vercel
       vercel --prod
       ```

**หมายเหตุ:** 
- Production deployments จะไม่ถูกลบอัตโนมัติ
- Preview deployments อาจถูกลบหลังจากไม่ใช้งาน
- ถ้าใช้ Production URL (เช่น `https://your-project.vercel.app`) จะไม่เจอ error นี้

---

## 📝 สรุปขั้นตอนทั้งหมด

### 1. Deploy บน Vercel
- ✅ สร้างบัญชี Vercel
- ✅ Import Git Repository
- ✅ ตั้งค่า Environment Variables
- ✅ Deploy

### 2. จด Domain ที่ Namecheap
- ✅ สร้างบัญชี Namecheap
- ✅ ค้นหาและซื้อ Domain
- ✅ ชำระเงิน

### 3. ตั้งค่า DNS
- ✅ ไปที่ Namecheap → Advanced DNS หรือ Nameservers
- ✅ ตั้งค่า Nameservers หรือ DNS Records ตามที่ Vercel แนะนำ

### 4. เชื่อม Domain กับ Vercel
- ✅ เพิ่ม Domain ใน Vercel Dashboard
- ✅ รอ DNS Propagation (5 นาที - 48 ชั่วโมง)
- ✅ ทดสอบเปิดเว็บไซต์

---

## 🔗 ลิงก์ที่เป็นประโยชน์

- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel Documentation - Domains](https://vercel.com/docs/concepts/projects/domains)
- [Namecheap Dashboard](https://www.namecheap.com/myaccount/)
- [DNS Checker](https://dnschecker.org)
- [What's My DNS](https://www.whatsmydns.net)

---

## ⚠️ หมายเหตุสำคัญ

1. **DNS Propagation:**
   - ใช้เวลา 5 นาที - 48 ชั่วโมง
   - ต้องรอให้ DNS propagate ก่อน domain จะทำงาน

2. **SSL Certificate:**
   - Vercel จะสร้าง SSL Certificate อัตโนมัติ
   - ใช้เวลา 5-10 นาทีหลังจาก DNS propagate แล้ว

3. **Environment Variables:**
   - อย่าลืมตั้งค่า Environment Variables ใน Vercel
   - อย่า commit `.env.local` file ขึ้น Git

4. **Auto Deploy:**
   - ทุกครั้งที่ push code ไป GitHub → Vercel จะ deploy อัตโนมัติ
   - ไม่ต้อง deploy เองทุกครั้ง

5. **Domain Renewal:**
   - Domain จะหมดอายุทุกปี (หรือตามที่ซื้อ)
   - เปิด Auto-Renew เพื่อต่ออายุอัตโนมัติ

---

## 📚 เอกสารเพิ่มเติม

- [VERCEL_DOMAIN_SETUP.md](./VERCEL_DOMAIN_SETUP.md) - คู่มือการ deploy และตั้งค่า domain แบบละเอียด
- [VERCEL_IMPORT_PROJECT.md](./VERCEL_IMPORT_PROJECT.md) - แก้ปัญหาเมื่อไม่เจอโปรเจกต์ใน Vercel

---

**🎉 เสร็จแล้ว! ตอนนี้คุณสามารถ deploy เว็บไซต์ผ่าน Vercel และใช้ domain ของตัวเองได้แล้ว!**
