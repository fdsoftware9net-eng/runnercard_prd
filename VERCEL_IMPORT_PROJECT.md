# 🔧 แก้ปัญหา: ไม่เจอโปรเจกต์ใน Vercel หลังจากติดตั้ง GitHub App

## ❓ ปัญหา

หลังจากติดตั้ง Vercel GitHub App แล้ว แต่ไม่เจอโปรเจกต์ใน Vercel Dashboard

## 💡 สาเหตุ

**การติดตั้ง Vercel GitHub App ไม่ได้สร้างโปรเจกต์อัตโนมัติ!**

GitHub App จะให้สิทธิ์ Vercel เข้าถึง repository ของคุณเท่านั้น แต่คุณต้องไปที่ Vercel Dashboard เพื่อ **import project เอง**

---

## ✅ วิธีแก้ไข: Import Project ใน Vercel

### ขั้นตอนที่ 1: ไปที่ Vercel Dashboard

1. ไปที่ [https://vercel.com](https://vercel.com)
2. **Login** ด้วย GitHub account เดียวกันที่ติดตั้ง GitHub App
3. หลังจาก Login แล้ว คุณจะเห็นหน้า Dashboard

### ขั้นตอนที่ 2: สร้างโปรเจกต์ใหม่

1. คลิกปุ่ม **"Add New..."** (มุมขวาบน)
2. เลือก **"Project"**

### ขั้นตอนที่ 3: Import Git Repository

1. คุณจะเห็นรายการ **Git Repositories** ที่ Vercel สามารถเข้าถึงได้
2. **ค้นหา repository** ที่ต้องการ:
   - พิมพ์ชื่อ repository ในช่องค้นหา (เช่น `runnercard_prd`)
   - หรือเลื่อนดูรายการทั้งหมด

3. **ตรวจสอบว่าเจอ repository หรือไม่:**
   - ✅ **เจอ:** คลิก **"Import"** ข้าง repository
   - ❌ **ไม่เจอ:** ดูส่วน "แก้ปัญหา: ไม่เจอ Repository"

### ขั้นตอนที่ 4: ตั้งค่า Project

หลังจากคลิก Import แล้ว:

1. **Project Name:**
   - Vercel จะตั้งชื่อให้อัตโนมัติ (สามารถเปลี่ยนได้)
   - เช่น `runnercard-prd`

2. **Framework Preset:**
   - Vercel จะ auto-detect เป็น **Vite** (ถูกต้องแล้ว)
   - ถ้าไม่ถูกต้อง เลือก **Vite** เอง

3. **Root Directory:**
   - ปล่อยว่างไว้ (ใช้ `.`)

4. **Build and Output Settings:**
   - **Build Command:** `npm run build` (ควรมีอยู่แล้ว)
   - **Output Directory:** `dist` (ควรมีอยู่แล้ว)
   - **Install Command:** `npm install` (ควรมีอยู่แล้ว)

### ขั้นตอนที่ 5: ตั้งค่า Environment Variables

**สำคัญมาก!** ต้องตั้งค่าก่อน deploy:

1. ในหน้า Configure Project คลิก **"Environment Variables"**
2. คลิก **"Add"** เพื่อเพิ่มตัวแปร

**เพิ่มตัวแปรที่ 1:**
- **Key:** `VITE_SUPABASE_URL`
- **Value:** ใส่ URL ของ Supabase project ของคุณ
- **Environment:** เลือกทั้งหมด (Production, Preview, Development)
- คลิก **"Save"**

**เพิ่มตัวแปรที่ 2:**
- **Key:** `VITE_SUPABASE_ANON_KEY`
- **Value:** ใส่ Anon Key ของ Supabase project ของคุณ
- **Environment:** เลือกทั้งหมด (Production, Preview, Development)
- คลิก **"Save"**

### ขั้นตอนที่ 6: Deploy

1. หลังจากตั้งค่า Environment Variables แล้ว คลิก **"Deploy"**
2. รอให้ build เสร็จ (ประมาณ 1-3 นาที)
3. เมื่อเสร็จแล้ว จะได้ลิงก์ Production URL เช่น:
   ```
   https://runnercard-prd.vercel.app
   ```

---

## 🔍 แก้ปัญหา: ไม่เจอ Repository

### ปัญหา: Repository ไม่แสดงใน Vercel

**สาเหตุที่เป็นไปได้:**

1. **Repository ยังไม่ได้เลือกใน GitHub App Settings**
2. **Repository อยู่ใน Organization ที่ไม่ได้เชื่อมต่อ**
3. **ต้อง Refresh หรือ Reconnect**

### วิธีแก้ไข

#### วิธีที่ 1: ตรวจสอบ GitHub App Settings

1. ไปที่ GitHub → **Settings** → **Applications** → **Installed GitHub Apps**
2. คลิก **"Vercel"**
3. ตรวจสอบ **"Repository access"**:
   - ✅ **All repositories** = จะเห็นทุก repository
   - ⚠️ **Only select repositories** = จะเห็นเฉพาะที่เลือกไว้

4. **ถ้าเลือก "Only select repositories":**
   - ตรวจสอบว่า `patawee25346/runnercard_prd` อยู่ในรายการหรือไม่
   - ถ้าไม่มี → คลิก **"Configure"** → เพิ่ม repository

#### วิธีที่ 2: Refresh Vercel Dashboard

1. ไปที่ Vercel Dashboard
2. คลิก **"Add New..."** → **"Project"**
3. คลิกปุ่ม **"Refresh"** หรือ **"Reconnect"** (ถ้ามี)
4. รอสักครู่ แล้วลองค้นหา repository อีกครั้ง

#### วิธีที่ 3: Reconnect GitHub Account

1. ไปที่ Vercel Dashboard → **Settings** → **Git**
2. คลิก **"Disconnect"** ที่ GitHub
3. คลิก **"Connect"** อีกครั้ง
4. Authorize Vercel อีกครั้ง
5. กลับไปที่หน้า Import Project แล้วลองค้นหา repository อีกครั้ง

#### วิธีที่ 4: ตรวจสอบ Repository Name

1. ไปที่ GitHub → ดู repository ที่ต้องการ
2. ตรวจสอบชื่อ repository ที่แท้จริง:
   - URL จะเป็น: `https://github.com/patawee25346/ชื่อ-repository`
   - ชื่อ repository = ส่วนหลัง `/` สุดท้าย

3. กลับไปที่ Vercel แล้วค้นหาด้วยชื่อที่ถูกต้อง

---

## 🎯 ตรวจสอบว่า Repository ถูกต้องหรือไม่

### ตรวจสอบ Repository Name

จากข้อมูลที่คุณให้มา:
- **Repository ที่เลือกใน GitHub App:** `patawee25346/runnercard_prd`
- **ชื่อโปรเจกต์ปัจจุบัน:** `runner-passes-admin-v2`

**คำถาม:** Repository `runnercard_prd` ใช่โปรเจกต์ที่คุณต้องการ deploy หรือไม่?

### วิธีตรวจสอบ

1. **ไปที่ GitHub:**
   ```
   https://github.com/patawee25346/runnercard_prd
   ```

2. **ตรวจสอบว่า:**
   - มีไฟล์ `package.json` หรือไม่
   - มีไฟล์ `vite.config.ts` หรือไม่
   - มีไฟล์ `vercel.json` หรือไม่

3. **ถ้าใช่:** ใช้ repository นี้ได้เลย
4. **ถ้าไม่ใช่:** 
   - ต้องหา repository ที่ถูกต้อง
   - หรือสร้าง repository ใหม่และ push code ไป

---

## 📝 สรุปขั้นตอนทั้งหมด

### ✅ ขั้นตอนที่ต้องทำ:

1. **ไปที่ Vercel Dashboard**
   - [https://vercel.com/dashboard](https://vercel.com/dashboard)

2. **คลิก "Add New..." → "Project"**

3. **ค้นหาและ Import Repository**
   - ค้นหา `runnercard_prd` (หรือชื่อ repository ที่ถูกต้อง)
   - คลิก "Import"

4. **ตั้งค่า Project**
   - Framework: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`

5. **ตั้งค่า Environment Variables**
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`

6. **Deploy**

---

## 🚨 ปัญหาที่พบบ่อย

### ❌ ปัญหา: ไม่เจอ Repository ในรายการ

**วิธีแก้:**
1. ตรวจสอบ GitHub App Settings → Repository access
2. ตรวจสอบว่า repository ถูกเลือกไว้หรือไม่
3. Refresh หรือ Reconnect GitHub account ใน Vercel

### ❌ ปัญหา: Repository ไม่มี code หรือไม่ใช่โปรเจกต์ที่ต้องการ

**วิธีแก้:**
1. ตรวจสอบว่า repository ที่เลือกถูกต้องหรือไม่
2. ถ้าไม่ถูกต้อง → หา repository ที่ถูกต้อง
3. หรือสร้าง repository ใหม่และ push code ไป

### ❌ ปัญหา: Build ล้มเหลว

**วิธีแก้:**
1. ตรวจสอบ Environment Variables ตั้งค่าครบหรือยัง
2. ตรวจสอบว่า `package.json` มี build script หรือไม่
3. ดู Build Logs ใน Vercel Dashboard เพื่อหาสาเหตุ

---

## 🔗 ลิงก์ที่เป็นประโยชน์

- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel Documentation - Import Project](https://vercel.com/docs/concepts/projects/overview)
- [GitHub Settings - Installed Apps](https://github.com/settings/installations)

---

## 💡 คำแนะนำเพิ่มเติม

1. **ใช้ชื่อ Repository ที่ชัดเจน:**
   - ใช้ชื่อที่บอกได้ว่าเป็นโปรเจกต์อะไร
   - เช่น `runner-passes-admin` แทน `runnercard_prd`

2. **ตรวจสอบก่อน Import:**
   - ตรวจสอบว่า repository มี code ครบถ้วนหรือไม่
   - ตรวจสอบว่า `package.json` และ `vercel.json` มีอยู่หรือไม่

3. **ตั้งค่า Environment Variables ทันที:**
   - อย่าลืมตั้งค่า Environment Variables ก่อน deploy
   - จะได้ไม่ต้อง deploy ใหม่

---

**🎉 หลังจาก Import Project แล้ว คุณจะเห็นโปรเจกต์ใน Vercel Dashboard และสามารถ deploy ได้เลย!**

